# OCC — OpenClaw Command Center
# Copy this file to .env and adjust values

# Server
OCC_HOST=127.0.0.1
OCC_PORT=3401

# OpenClaw workspace path (where TOOLS.md, SOUL.md, etc. live)
# Defaults to $HOME/.openclaw/workspace if not set
OCC_WORKSPACE=

# Display names (shown in the UI)
OCC_HUMAN_NAME=Human
OCC_AGENT_NAME=Agent

# Default assignee for new tasks ("human" or "agent")
OCC_DEFAULT_ASSIGNEE=agent

# Default comment author name
OCC_DEFAULT_AUTHOR=Human
